/**
 * App-wide configuration constants.
 *
 * AUTH_REQUIRED — when true, all routes require a valid session.
 * Set to false to allow guest browsing of the workspace.
 */
export const AUTH_REQUIRED = false
